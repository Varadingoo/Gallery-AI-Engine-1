name: Forge Android Vulkan Engine

on: 
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-22.04

    steps:
      - name: 1. Clone the FOSS Engine
        uses: actions/checkout@v4
        with:
          repository: 'leejet/stable-diffusion.cpp'
          submodules: 'recursive'

      - name: 2. Setup the Android NDK
        uses: nttld/setup-ndk@v1
        with:
          ndk-version: r26b

      - name: 3. Install Vulkan SDK & Link Headers
        run: |
          wget -qO- https://packages.lunarg.com/lunarg-signing-key-pub.asc | sudo tee /etc/apt/trusted.gpg.d/lunarg.asc
          sudo wget -qO /etc/apt/sources.list.d/lunarg-vulkan-jammy.list http://packages.lunarg.com/vulkan/lunarg-vulkan-jammy.list
          sudo apt-get update
          sudo apt-get install -y vulkan-sdk ninja-build
          sudo cp -r /usr/include/vulkan/* $ANDROID_NDK_HOME/toolchains/llvm/prebuilt/linux-x86_64/sysroot/usr/include/vulkan/
          sudo cp -r /usr/include/vk_video $ANDROID_NDK_HOME/toolchains/llvm/prebuilt/linux-x86_64/sysroot/usr/include/

      - name: 4. Compile the Engine
        run: |
          mkdir build && cd build
          cmake -G Ninja -DCMAKE_TOOLCHAIN_FILE=$ANDROID_NDK_HOME/build/cmake/android.toolchain.cmake -DANDROID_ABI=arm64-v8a -DANDROID_PLATFORM=android-29 -DGGML_VULKAN=ON -DSD_VULKAN=ON -DSD_BUILD_EXAMPLES=OFF -DSD_BUILD_TESTS=OFF ..
          ninja

      - name: 5. Collect and Upload Artifacts
        run: |
          mkdir -p output_libs
          # This finds every compiled .a file and copies it to our output folder
          find build -name "*.a" -exec cp {} output_libs/ \;
          
      - name: 6. Hand over to Developer
        uses: actions/upload-artifact@v4
        with:
          name: Android-Vulkan-Engine
          path: output_libs/*.a
